// Project are assigned a UniqId() when created, that is mirrored into a data attribute. 

// The active project will be queried for it's data attribute to be used as the LIST ID entry 
// in the task object. 

// Completed tasks will have their LIST ID changed to L0002 to be kept as a record.

// The 'All Tasks' project will have a List ID of L0001 and will be active on first load.